apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: genesis-token-bootstrap
  namespace: argocd
  annotations:
    argocd.argoproj.io/hook: PreSync
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["create"]
    resourceNames: ["juno-auth-secret"]
